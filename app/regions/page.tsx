import Link from 'next/link'
import { KnowledgeHeader } from '@/components/knowledge/research-index'
import { regions } from '@/lib/knowledge'
export default function RegionsPage() { return <main className="min-h-screen bg-background text-foreground"><KnowledgeHeader eyebrow="Knowledge / Regions" title="Context changes the question." description="Regional indexes for understanding how policy, infrastructure, and participation conditions differ across the continent." /><section className="mx-auto max-w-6xl px-6 pb-24 md:px-10"><div className="grid gap-px border border-border bg-border md:grid-cols-3">{regions.map((region) => <Link key={region.id} href={`/regions/${region.slug}`} className="bg-background p-7 hover:bg-muted/40"><p className="font-mono text-xs uppercase tracking-[0.18em] text-muted-foreground">Region</p><h2 className="mt-6 font-serif text-3xl">{region.name}</h2><p className="mt-4 text-sm leading-6 text-muted-foreground">{region.description}</p></Link>)}</div></section></main> }
