import Link from 'next/link'
import { KnowledgeHeader } from '@/components/knowledge/research-index'
import { projects } from '@/lib/knowledge'
export default function ProjectsPage() { return <main className="min-h-screen bg-background text-foreground"><KnowledgeHeader eyebrow="Knowledge / Projects" title="Research becomes useful when it can be entered." description="Explore the AKSOS projects that turn complex environments into clearer pathways for understanding and participation." /><section className="mx-auto max-w-6xl px-6 pb-24 md:px-10"><div className="grid gap-px border border-border bg-border md:grid-cols-2">{projects.map((project) => <Link key={project.id} href={`/projects/${project.slug}`} className="bg-background p-8 hover:bg-muted/40"><p className="font-mono text-xs uppercase tracking-[0.18em] text-muted-foreground">Project</p><h2 className="mt-6 font-serif text-4xl">{project.name}</h2><p className="mt-5 max-w-xl leading-7 text-muted-foreground">{project.description}</p></Link>)}</div></section></main> }
